## 1. Course Setup and Source Verification

- [x] 1.1 Verify the current OpenSpec runtime requirements, Homebrew and npm installation commands, coding-agent invocations, CLI commands, and official resource URLs against primary sources.
- [x] 1.2 Create the English `OpenSpec` course Markdown with a generated short description, suitable tags, and references to exactly three lessons in the required order.

## 2. Lesson Content

- [x] 2.1 Write the introductory lesson covering OpenSpec's purpose, the problems it solves, specification-driven development, macOS installation through Homebrew and npm, verification, initialization, and official project links.
- [x] 2.2 Write the coding-agent lesson distinguishing terminal and chat commands and walking through the Codex-first explore, propose, review, apply, and archive workflow with a note about other agents' syntax.
- [x] 2.3 Write the internals lesson covering the npm-distributed CLI, core setup and inspection commands, artifact dependencies, the `openspec/` file tree, active and archived changes, and generated agent-integration files.
- [x] 2.4 Review the course overview and all three lessons, wrapping every important OpenSpec term in Markdown inline-code formatting at its defining occurrence without styling routine repetitions.
- [x] 2.5 Expand the coding-agent workflow lesson with a post-apply review and correction loop that distinguishes unfinished tasks from defects or planning gaps discovered after completion, explains how `$openspec-update-change` revises affected artifacts or adds corrective tasks, and requires `$openspec-apply-change` plus verification before archive.

## 3. Content Verification

- [x] 3.1 Confirm that each lesson covers one bounded topic, takes no more than 30 minutes, uses consistent English terminology, and contains only commands and links supported by the verified sources.
- [x] 3.2 Confirm that the published course has exactly the required lesson order and includes no separate prerequisites section, installation guide, or cheatsheet.
- [x] 3.3 Run the repository content checks, tests, and production build and resolve any failures introduced by the new course files.
- [x] 3.4 Verify that important definitions render with inline-code emphasis and rerun the repository content checks, tests, and production build after the formatting update.
- [x] 3.5 Verify that the revised lesson presents a complete, non-linear workflow with correct skill authority boundaries and invocation syntax, then rerun the content checks, tests, and production build.
