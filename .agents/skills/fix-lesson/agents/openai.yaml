interface:
  display_name: "Fix Lesson"
  short_description: "<lesson-slug> <fix-instruction> — Fix an existing lesson"
  default_prompt: "Use $fix-lesson <lesson-slug> <fix instruction> to correct an existing lesson in English and Russian."
